<%= form_for @team do |f| %>
  <%= render 'shared/error_messages', :object => f.object %>
  <p>
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </p>
    <p>
      <%= f.label :admin %><br />
      <%= f.text_field :admin %>
    </p>

  <p><%= f.submit %></p>

<% end %>
