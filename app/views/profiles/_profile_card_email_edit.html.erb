<h3>Email</h3>
<a class="save" href="">save</a>
<%= form_for @profile do |f| %>
  <%= text_field :user, :email, :value => current_user.email, :size => 20 %>
  <% if @profile_update_error.present? %>
    <div><%= @profile_update_error %></div>
  <% end %>
  <%= hidden_field_tag "profile_card", "email" %>
<% end %>