<h2>I am offering for free<span class="save">save</span></h2>
<%= form_for @profile do |f| %>
  <p><%= f.text_area :offering, :rows => 0 %></p>
  <%= hidden_field_tag "profile_tab_info", "offering_free" %>
<% end %>